/**
 * Global setup file - runs once before all tests
 * Empty by default, can be extended for project-wide setup
 */

export default async () => {
  // Setup code runs here if needed in future
  console.log('Running global setup...');
};

